# Kore 领域上下文

本仓库设计并实现 Kore —— 一门面向底层系统编程的通用语言。

## 词汇表

设计与实现讨论中一律使用下列术语，不使用括注的同义词。

### 语言概念

- **编译期绑定（comptime binding）** — 用 `::` 声明的绑定。顺序无关，在编译期求值。类型、函数、常量、泛型都属于此类。不叫"常量"或"静态声明"。
- **运行期绑定（runtime binding）** — 用 `:` 或 `:=` 声明的绑定。顺序敏感。不叫"变量声明"。
- **可变标记（mut marker）** — 记号 `~`。出现在被它修饰的名字或类型之前。不叫"mut 关键字"。
- **分支（branch）** — 记号 `?` 引出的构造。它统一了其他语言的 if / else / switch / match，因此**不要**把它称作"if 语句"或"match 表达式"，一律叫分支。
- **循环（loop）** — 记号 `@` 引出的构造。统一了 for / while / foreach。不要叫"for 循环"。
- **臂（arm）** — 分支中 `模式 => 表达式` 的一项。
- **守卫（guard）** — 只有单臂且无花括号的分支，形如 `? cond => stmt`。
- **联合（union）** — 用 `|` 声明的和类型（sum type）。不叫 enum，也不叫 tagged union。
- **变体（variant）** — 联合的一个分支，如 `.Num`。
- **借用指针（borrow pointer）** — `^T`，不拥有所指对象。
- **所有指针（own pointer）** — `own ^T`，唯一所有，出作用域自动 drop。
- **不逃逸检查（escape check）** — 保证借用指针不存入更长寿位置的编译期检查。Kore 没有生命周期变量，所以**不要**说"借用检查器"或"lifetime"。
- **错误联合（error union）** — 类型 `T ! E`。不叫 Result。
- **传播（propagate）** — 后缀 `!`。不叫 try 或 unwrap。
- **保证尾调用（guaranteed tail call）** — 关键字 `jmp`。不叫 TCO，因为它是语义保证而非优化。

### 实现阶段

- **Kore0** — 自举用的最小子集，由宿主语言实现的 stage0 编译器接受。
- **stage1** — 用 Kore0 写成、由 stage0 编译的编译器。
- **stage2** — 用完整 Kore 写成、由 stage1 编译的编译器。自举闭合的判据是 stage2 与 stage3 的产物逐字节相同。
- **stage3** — stage2 编译同一份完整 Kore 源码的产物。stage2 与 stage3 逐字节相同即自举闭合。
- **freestanding** — 不依赖 libc、不依赖宿主 OS 的目标配置。

### 测试

- **测试注解（test annotation）** — 记号 `--~`，写在触发诊断的那一行行尾，声明期望的错误码与文本。编译器眼里是普通注释，只有 runner 认识。不叫"期望注释"。
- **契约断言（contract assertion）** — 记号 `--=`，声明一条机器级语义保证（如 `--= tailcall f`、`--= volatile-load u32`），由编译器在对应 pass 的检查点验证。不比对汇编文本，所以**不要**叫"汇编断言"或"快照"。注意与 `--!`（模块级文档注释，见 ADR 008）不是同一个记号。
- **便宜代理（cheap proxy）** — 完整判据的低成本近似，用于放进快时间预算的层。目前只有一个：同一文件编译两次比对字节，代理完整的 stage2 == stage3 不动点。
- **层预算（layer budget）** — 定义测试分层的时间上限（第一层 ≤ 60 秒、第二层 ≤ 15 分钟、深度层无上限）。分层由预算定义，**不由** CI 产品定义，所以不要说"CI 阶段"或"pipeline"。
- **`test_exit`** — `bare/` 提供的内置，把退出码变成 QEMU 下的关机序列。freestanding 目标唯一的目标特定测试设施。

## 规范文档

`docs/spec/` 下按主题拆分，01 概览、02 语法、03 类型、04 对象模型、05 内存与错误、06 底层能力、07 示例。
